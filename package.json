{
  "name": "local-storage-HW",
  "version": "1.0.0",
  "homepage": "https://YehorBr.github.io/local-storage-HW",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html --public-url ./",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "gh-pages": "^6.0.0"
  }
}
